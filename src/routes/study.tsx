import { Component, onMount } from "solid-js";
import { useSakarboContext } from "~/app";

const Study: Component = (_props: any) => {
  let context = undefined;
  // onMount(() => {
  //   context = useSakarboContext();
  // });
  return (
    <div>
      <div>study</div>
    </div>
  );
};

export default Study;
